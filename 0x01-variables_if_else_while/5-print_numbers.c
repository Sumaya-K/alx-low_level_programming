#include <stdio.h>

/**
 * main - prints single digits until 10
 *
 * Return: Always 0
 */

int main(void)
{
	int x = 0;

	while (x < 10)
	{
		printf("%d", x);
		x++;
	}
	printf("\n");
	return (0);
}
