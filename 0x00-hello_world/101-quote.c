#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	fprintf(stderr, "and that piece of art is useful\" - Dora Korpar, %d-%d-%d\n", "2015", "10", "19")
	return (1)
}
